# Ripresa lavori: 2026-07-29

Nota scritta alla chiusura della sessione del 2026-07-28.

## Dove siamo

Task 7 (re-review) e Task 8, 9, 10, 11, 12 sono conclusi **dentro il repository**. Il progetto **non** e' completo: mancano due azioni esterne che richiedono credenziali, e tutte le verifiche di produzione dipendono da quelle.

Stato Git:

```text
main                                  1503b25  feat:release-seven-hour-bilingual-interview-sprint
feature/seven-hour-bilingual-sprint   1503b25  (stesso commit, merge in fast-forward gia' eseguito)
origin/main                           8ac8eea  (non ancora aggiornato)
```

Entrambi i worktree sono puliti. `main` e' avanti di 22 commit su `origin/main`, in fast-forward puro: nessun rebase, nessun force push necessario.

Verifiche eseguite su `main` dopo il merge:

| Comando | Esito |
|---|---|
| `npm run lint` | Exit 0 |
| `npm test` | 143 passati, 0 falliti |
| `npm run build` | Exit 0 |
| `git diff --check` | Exit 0 |
| `git status --short` | pulito |

Output di build:

```text
Build validata: 6 moduli, contenuto versione 2, PWA installabile
Minuti totali: 420 (teoria 189, casi e pratica 231, quota pratica 55%)
Parole di teoria: 34892 italiane, 33059 inglesi (95% dell'italiano)
Fonti: 32 citate su 33 nel catalogo, tutte risolte
Localizzazione: completa in italiano e inglese su ogni campo visibile
```

## Da fare domani, in questo ordine

### 1. Push (serve la tua sessione GitHub)

Dalla root del repository:

```powershell
Set-Location 'E:\Lavoro\Apprendimento IA'
git status --short          # deve essere vuoto
git log -1 --oneline        # deve mostrare 1503b25
git push origin main
```

Motivo del blocco: nel mio ambiente di esecuzione `git push` fallisce con
`fatal: could not read Username for 'https://github.com'`. Sono assenti credential helper, `~/.netrc`, `~/.git-credentials`, chiavi SSH, la CLI `gh` e qualunque token in ambiente. La rete funziona: `git fetch origin` riesce.

### 2. Migrazione Supabase (serve il tuo login)

Accedi a <https://supabase.com/dashboard/project/ifzcbqxjqfinrzkguqzs/sql/new>. La sessione del browser collegato non era autenticata e reindirizzava alla pagina di sign-in.

Esegui:

```sql
alter table public.lesson_progress
add column if not exists content_version integer not null default 1
check (content_version >= 1);
```

Poi verifica:

```sql
select column_name, data_type, column_default
from information_schema.columns
where table_schema = 'public'
  and table_name = 'lesson_progress'
  and column_name = 'content_version';
```

Atteso: una colonna `integer` con default `1`.

### 3. Test end-to-end sulla tua macchina

```powershell
npm run test:e2e
```

Richiede Playwright e Microsoft Edge. Nel mio ambiente fallisce solo per il binario del browser mancante e il download e' bloccato. L'import di Playwright ora prova prima il pacchetto e poi il percorso assoluto registrato, quindi funziona ovunque sia installato.

### 4. Cosa faccio io appena i punti 1 e 2 sono fatti

- Verifica in browser della produzione su <https://fabrizioborgomastro.github.io/ia-apprendimento/>: nuova asset version `v5`, una sola unita' visibile, cambio lingua, ricarica di un deep link, console pulita.
- Controllo viewport a 360, 390, 768 e 1440 px.
- Verifica cache PWA e pulizia delle cache precedenti.
- Test cross-device PC e telefono con sessione Gmail.
- Test RLS: un secondo utente non deve leggere le righe del primo.
- Decisione finale su `withLegacyProjection`, il cui gate di rimozione richiede un E2E verde.

## Cosa e' stato consegnato

- Modulo 5 `mvp-governance`: 8 unita', 75 minuti, 6.746 parole italiane, canvas MVP, registro rischi, RACI, checklist di scaling, tutti ricalcolabili.
- Modulo 6 `interview-lab`: 8 unita', 75 minuti, 3.609 parole italiane, simulazione da 20 minuti, rubric 0-2 su sei criteri, tracciatore di prontezza, scheda di ripasso rapido, 11 risposte da colloquio.
- Curriculum assemblato in `public/content/index.js`, con `public/content.js` ridotto a un re-export.
- Nuovo navigatore a unita' singola con deep link `/lesson/:slug?unit=:unitId` e selettore Italiano/English persistente.
- PWA `ai-sprint-v5` con precache dell'intero grafo dei moduli e test che lo impone.

Il conflitto di specifica su Modulo 6 (sei unita' e 75 minuti sono incompatibili con il limite di 5-10 minuti per unita') e' documentato in `.superpowers/sdd/2026-07-28-seven-hour-bilingual-interview-sprint/task-9-brief.md`. Risolto con otto unita', senza indebolire alcuna soglia.

## Difetti trovati in review e corretti

1. Modulo 5: gli artefatti incorporati nei casi potevano divergere da quelli esportati dalla lezione senza che nulla se ne accorgesse.
2. Modulo 6: l'unita' che insegna a non fare la media dei punteggi citava 10,7 mentre il suo stesso tracciatore dava 10,6.
3. Navigatore: nove unita' dei Moduli 1 e 2 non hanno attivita' e, con la regola iniziale, non potevano essere completate. Bloccava l'avanzamento sui primi due moduli.

## Dove sono i dettagli

`.superpowers/sdd/2026-07-28-seven-hour-bilingual-interview-sprint/`

- `progress.md`: ledger completo dei task.
- `task-8-report.md` ... `task-12-report.md`: evidenza RED e GREEN, ricalcoli indipendenti, fix round, concern.
- `task-8-brief.md`, `task-9-brief.md`: requisiti quantitativi e allocazioni concordate prima della scrittura.

Questa cartella e' esclusa da Git per scelta del progetto, quindi vive solo su disco nel worktree.
